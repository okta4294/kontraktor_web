services:
  - type: web
    name: kontraktor-backend
    env: docker
    rootDir: backend
    plan: free
    healthCheckPath: /auth/me
    envVars:
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
